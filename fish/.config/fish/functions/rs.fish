function rs --wraps='for dir in $PATH; ls $dir 2>/dev/null; end | sort -u | fzf --preview \'pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\\033[33mLocal excutable, not managed by pacman.\\033[0m\\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi\' --bind \'enter:execute(pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\\033[33mLocal excutable, not managed by pacman.\\033[0m\\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi | less -R)\'' --wraps='for dir in $PATH; ls $dir 2>/dev/null; end | sort -u | fzf --layout=reverse --preview \'pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\\033[33mLocal excutable, not managed by pacman.\\033[0m\\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi\' --bind \'enter:execute(pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\\033[33mLocal excutable, not managed by pacman.\\033[0m\\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi | less -R)\'' --description 'alias rs for dir in $PATH; ls $dir 2>/dev/null; end | sort -u | fzf --layout=reverse --preview \'pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\\033[33mLocal excutable, not managed by pacman.\\033[0m\\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi\' --bind \'enter:execute(pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\\033[33mLocal excutable, not managed by pacman.\\033[0m\\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi | less -R)\''
    for dir in $PATH; ls $dir 2>/dev/null; end | sort -u | fzf --layout=reverse --preview 'pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\033[33mLocal excutable, not managed by pacman.\033[0m\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi' --bind 'enter:execute(pkg=$(paru -Qqo {} 2>/dev/null); if [ -n "$pkg" ]; then paru -Qil "$pkg"; else fullpath=$(command -v {}); echo -e "\033[33mLocal excutable, not managed by pacman.\033[0m\n"; file "$fullpath" 2>/dev/null; ls -lh "$fullpath" 2>/dev/null; fi | less -R)' $argv
end
